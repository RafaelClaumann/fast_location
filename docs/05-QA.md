# Q&A - Conceitos Dart & Flutter

## 1. O que é o 'part' e 'part of'?

No Dart, o conceito de **Biblioteca (Library)** é a unidade básica de modularização. Nem sempre uma biblioteca corresponde a apenas um arquivo físico. As palavras-chave `part` e `part of` permitem que você defina uma única biblioteca que se estende por múltiplos arquivos.

### Como funciona (O conceito):
Diferente do `import`, que estabelece uma dependência entre duas bibliotecas distintas, o `part` é como se você estivesse "colando" o código de um arquivo secundário dentro do principal antes da compilação.

- **Escopo Único:** Arquivos que fazem parte da mesma biblioteca compartilham absolutamente tudo, inclusive membros privados (que começam com `_`).
- **Restrições:** 
    - Apenas o arquivo principal (o "host") pode ter as declarações de `import` ou `library`.
    - Os arquivos secundários (`part of`) não podem importar nada; eles herdam os imports do arquivo principal.

### Uso comum:
É o pilar da **Geração de Código**. Ferramentas como `json_serializable` ou `freezed` geram arquivos `.g.dart` que são definidos como `part` do seu arquivo original. Isso permite que o código gerado acesse seus construtores privados e vice-versa, mantendo o código limpo.

---

## 2. Como o Dart lida com async/await e Futures?

A dúvida comum é: *Sempre preciso usar `async` e `await` quando lido com `Future`?*

### O conceito por trás:
O Dart é **single-threaded** e utiliza um **Event Loop**. Quando uma função é marcada como `async`, o Dart automaticamente a transforma em uma "Máquina de Estados".

No seu exemplo:
```dart
Future<Box<AddressModel>> _openBox() async {
  return await Hive.openBox<AddressModel>(_boxName);
}
```

**O que acontece internamente:**
1. O `async` avisa ao Dart: "Prepare-se, vou suspender a execução em algum momento".
2. O `await` diz: "Pare tudo aqui, espere o `Future` do Hive terminar, extraia o valor (`Box`) e então continue".
3. Após o valor ser extraído, o `async` o embrulha novamente em um **novo** `Future` para retornar.

### Por que pode ser redundante?
Se você simplesmente retorna o resultado de outra função que já devolve um `Future`, você pode fazer um **encaminhamento direto**:

```dart
Future<Box<AddressModel>> _openBox() {
  return Hive.openBox<AddressModel>(_boxName);
}
```

**Por que isso funciona?**
Aqui, você não está esperando o valor ser "desembrulhado". Você está apenas passando o "ticket" (o `Future`) adiante. Quem chamou `_openBox` é quem vai decidir quando dar o `await`. 

**Vantagens da forma direta:**
- **Performance:** Você evita a criação da máquina de estados do `async` e a criação de um segundo `Future` desnecessário.
- **Stack Trace:** Em alguns cenários de erro, o stack trace fica mais limpo pois há uma camada a menos de "suspensão".

### Quando o async/await é OBRIGATÓRIO?
- **Tratamento de Erros:** Se você precisa de um `try/catch` ao redor da chamada assíncrona.
- **Lógica Sequencial:** Se você precisa do valor de um `Future` para decidir o que fazer na linha seguinte.
- **Mudança de Tipo:** Se a função retorna `Future<String>` mas você está chamando algo que retorna `Future<int>` e precisa converter o valor.

---

## 3. Por que o `part 'address_model.g.dart'` não funciona?

A diretiva `part 'address_model.g.dart'` referencia um arquivo que precisa ser **gerado pelo `build_runner`**. O erro ocorre porque esse arquivo ainda não existe no disco.

### Como funciona:
O Hive usa a anotação `@HiveType` junto com `hive_generator` para gerar automaticamente o `TypeAdapter` da sua classe. O fluxo é:

1. Você escreve o modelo com as anotações (`@HiveType`, `@HiveField`).
2. Você declara `part 'address_model.g.dart'` apontando para o arquivo que **será** gerado.
3. O `build_runner` lê as anotações e **cria** o arquivo `.g.dart` com o `AddressModelAdapter`.

### Como resolver:
Execute o comando de geração na raiz do projeto:

```bash
dart run build_runner build
```

Ou, para forçar a recriação em caso de conflito:

```bash
dart run build_runner build --delete-conflicting-outputs
```

### Por que as dependências já estão prontas:
O `pubspec.yaml` já possui `hive_generator` e `build_runner` em `dev_dependencies`, então basta rodar o comando acima para o arquivo ser gerado em `lib/src/models/address_model.g.dart`.

### Onde o arquivo gerado fica?
Diferente do Java (Lombok, MapStruct), que gera código em `target/generated-sources/` fora do seu código-fonte, o Dart gera o arquivo **ao lado do arquivo original**:

```
lib/src/models/
├── address_model.dart        ← você escreve
└── address_model.g.dart      ← gerado pelo build_runner
```

| Conceito | Java | Dart |
|---|---|---|
| Ferramenta | `javac` + `annotation processor` | `build_runner` |
| Onde gera | `target/generated-sources/` | Mesmo diretório do fonte |
| Arquivo gerado | `.class` ou novo `.java` | `.g.dart` |
| Equivalente a | Lombok / MapStruct / Avro | `hive_generator`, `json_serializable`, `freezed` |
| Versionado no git | Não (ignorado via `.gitignore`) | Opcional, mas geralmente não |

> **Dica:** O `.g.dart` normalmente é adicionado ao `.gitignore`, assim como o `target/` no Java, pois é um artefato de build que pode ser sempre regenerado.

### Como adicionar ao .gitignore:
Adicione a linha abaixo na seção de Flutter/Dart do `.gitignore`:

```gitignore
# Generated files (build_runner)
*.g.dart
```

O padrão `*.g.dart` cobre todos os arquivos gerados em qualquer subdiretório do projeto, sem precisar listar cada um individualmente — equivalente a ignorar `target/` no Java.

---

## 4. Por que chamamos `WidgetsFlutterBinding.ensureInitialized()` no `main()`?

No projeto, o `main()` é marcado como `async` e executa operações antes de chamar o `runApp`:

```dart
void main() async {
  WidgetsFlutterBinding.ensureInitialized();

  await Hive.initFlutter();
  Hive.registerAdapter(AddressModelAdapter());

  runApp(const MyApp());
}
```

A dúvida natural é: *para que serve essa chamada antes do `runApp`?*

### O problema que ela resolve:

O Flutter precisa de uma "ponte" entre o código Dart e o motor nativo (Android/iOS). Essa ponte é o **`WidgetsFlutterBinding`**. Normalmente, o `runApp` cria essa ponte automaticamente como seu primeiro passo.

O problema surge quando você precisa chamar código de plataforma **antes** do `runApp`. É exatamente o caso aqui: `Hive.initFlutter()` usa o `path_provider` por baixo dos panos para descobrir o diretório de armazenamento do dispositivo — o que exige que a ponte com o nativo já esteja ativa.

Sem a chamada, a execução de `await Hive.initFlutter()` tentaria acessar a camada nativa com a ponte inexistente, lançando um erro em tempo de execução.

### O que ela faz exatamente:

`ensureInitialized()` inicializa o `WidgetsFlutterBinding` caso ele ainda não tenha sido inicializado. O nome "ensure" é intencional: a chamada é idempotente — se a ponte já existir (porque `runApp` já foi chamado, por exemplo), ela não faz nada e não causa erro.

### Analogia com Java/Spring:

É semelhante a precisar do `ApplicationContext` do Spring antes do servidor HTTP subir. Se você quer acessar um bean (ex: um repositório) no bloco de inicialização, o contexto precisa estar pronto primeiro. `ensureInitialized()` é o equivalente a garantir que o contexto da plataforma Flutter está ativo antes de usá-lo.

### Quando é obrigatório:

| Situação | Precisa? |
|---|---|
| Código assíncrono antes do `runApp` que acessa a plataforma (ex: Hive, SharedPreferences, Firebase) | **Sim** |
| `main()` é `async` mas só usa lógica Dart pura (sem plugins) | Não obrigatório, mas recomendado |
| `runApp` é a primeira e única chamada no `main()` | Não (o próprio `runApp` inicializa) |

> **Regra prática:** Se o seu `main()` é `async` e chama qualquer plugin ou pacote que acessa recursos nativos antes do `runApp`, adicione `WidgetsFlutterBinding.ensureInitialized()` como **primeira linha**.

---

## 5. O que são os "Controllers" no Flutter (ex: `_cepController`)?

No projeto, o campo de texto do CEP é controlado assim:

```dart
class _HomePageState extends State<HomePage> {
  final _cepController = TextEditingController();

  // ...

  @override
  Widget build(BuildContext context) {
    return TextField(
      controller: _cepController,
      // ...
    );
  }

  void _searchCep() {
    final cep = _cepController.text; // lê o valor atual
    _cepController.clear();          // limpa o campo
  }

  @override
  void dispose() {
    _cepController.dispose(); // libera os recursos
    super.dispose();
  }
}
```

### O conceito por trás:

Um `TextEditingController` é um objeto que vive **fora da árvore de widgets** e serve como canal bidirecional entre o seu código Dart e o campo de texto na tela. Em vez de depender de callbacks para cada tecla digitada, você tem uma referência direta ao estado do campo.

Ele oferece três operações principais:

- **`.text`** — lê o valor atual digitado no campo.
- **`.text = 'valor'`** — escreve um valor no campo programaticamente.
- **`.clear()`** — esvazia o campo.

### O controller é filho do `TextField`?

Não. O `TextEditingController` **não é um widget** e não faz parte da árvore de widgets — ele é um objeto Dart comum que existe em paralelo. A relação entre os dois é de **injeção de dependência**: você cria o controller no `State`, e o passa para o `TextField` pelo parâmetro `controller:`.

```
_HomePageState
├── _cepController (TextEditingController) ← objeto Dart, fora da árvore
└── build()
    └── Scaffold
        └── TextField
            └── controller: _cepController  ← referência injetada
```

O `TextField` usa o controller internamente para sincronizar o que o usuário digita com o objeto. A partir daí, qualquer leitura ou escrita feita via `_cepController` reflete imediatamente no campo visual, e vice-versa.

Isso é diferente de, por exemplo, um `Padding` que envolve um `Text` — nesses casos há uma relação pai/filho real na árvore. O controller é mais parecido com um serviço injetado: ele existe fora da hierarquia visual e é compartilhado por referência.

### Por que ele precisa do `dispose()`?

O `TextEditingController` aloca recursos internos (listeners, streams) que **não são liberados automaticamente** pelo garbage collector do Dart, pois ele mantém referências vivas enquanto o widget existir. Chamar `dispose()` no método `dispose()` do `State` garante que esses recursos sejam liberados quando a tela for removida da navegação — evitando memory leaks.

### Analogia com Java/Android:

É equivalente ao par `EditText` + `TextWatcher` do Android clássico, mas invertido: em vez de o campo chamar seu código a cada mudança, você guarda uma referência ao controlador e consulta o valor quando precisar.

| Conceito | Android (Java/Kotlin) | Flutter (Dart) |
|---|---|---|
| Campo de texto | `EditText` | `TextField` |
| Leitura do valor | `editText.getText().toString()` | `_controller.text` |
| Escrita de valor | `editText.setText("valor")` | `_controller.text = "valor"` |
| Limpeza | `editText.setText("")` | `_controller.clear()` |
| Liberação de recursos | `removeTextChangedListener(...)` | `_controller.dispose()` |

### Quando usar vs. alternativas:

O `TextEditingController` é ideal quando você precisa **ler ou manipular** o valor do campo de fora do próprio campo (ex: ao clicar em um botão). Se você só precisa **reagir** a cada mudança enquanto o usuário digita, o parâmetro `onChanged` do `TextField` é suficiente e mais simples — sem necessidade de criar um controller.

```dart
// Alternativa mais simples quando só precisa reagir à digitação:
TextField(
  onChanged: (value) => print(value),
)
```

> **Regra prática:** Criou um `TextEditingController`? Sempre chame `_controller.dispose()` no `dispose()` do `State`. Esquecer esse passo é uma das fontes mais comuns de memory leak em apps Flutter.

---

## 6. Como funciona o `itemBuilder` do `ListView.builder`?

No projeto, a lista de endereços é renderizada assim:

```dart
ListView.builder(
  itemCount: _recentAddresses.length,
  itemBuilder: (context, index) {
    return AddressCard(
      address: _recentAddresses[index],
      onDelete: () {
        setState(() {
          _recentAddresses.removeAt(index);
        });
      },
    );
  },
),
```

### O conceito por trás:

`ListView.builder` não cria todos os widgets de uma vez. Ele usa **renderização preguiçosa (lazy rendering)**: só constrói o widget de um item quando ele está prestes a aparecer na tela, e o descarta quando sai da área visível. Para listas longas, isso é uma grande vantagem de performance.

O parâmetro `itemBuilder` é uma **função de callback** que o Flutter chama repetidamente — uma vez para cada item que precisa ser exibido. Sua assinatura é:

```dart
Widget Function(BuildContext context, int index)
```

O Flutter gerencia o loop internamente. Você não escreve `for` nem `while` — você apenas descreve *como construir um item dado seu índice*, e o framework chama essa função quantas vezes forem necessárias.

### O que são `context` e `index`:

- **`context`** (`BuildContext`): representa a posição deste widget específico na árvore de widgets. É necessário para acessar temas, navegação e outros recursos do Flutter que dependem de onde o widget está na hierarquia.
- **`index`** (`int`): o índice do item atual, de `0` até `itemCount - 1`. É com ele que você acessa o elemento correspondente na lista: `_recentAddresses[index]`.

### Por que `index` funciona no `onDelete`:

O `index` dentro do `onDelete` é capturado pelo **closure** do `itemBuilder`. Cada vez que o Flutter chama `itemBuilder(context, 2)`, por exemplo, a função `onDelete` gerada naquela chamada "lembra" que `index == 2`. Isso é o mesmo mecanismo de closures do Java com lambdas — a diferença é que no Dart não há a restrição de `effectively final` para variáveis capturadas de fora.

### Analogia com Java/Android:

É o equivalente direto ao `RecyclerView.Adapter` do Android, mas sem o boilerplate:

| Conceito | Android (`RecyclerView`) | Flutter (`ListView.builder`) |
|---|---|---|
| Total de itens | `getItemCount()` | `itemCount:` |
| Construir item | `onCreateViewHolder` + `onBindViewHolder` | `itemBuilder:` (tudo em um só lugar) |
| Lazy rendering | Sim (com `RecycledViewPool`) | Sim (nativo) |
| Acesso ao item | `list.get(position)` | `list[index]` |
| Contexto/posição | `ViewHolder` + `position` | `context` + `index` |

### `ListView` vs `ListView.builder`:

```dart
// ListView comum — cria TODOS os widgets de uma vez (ruim para listas longas):
ListView(
  children: _recentAddresses.map((a) => AddressCard(address: a)).toList(),
)

// ListView.builder — cria só o que está visível (recomendado):
ListView.builder(
  itemCount: _recentAddresses.length,
  itemBuilder: (context, index) => AddressCard(address: _recentAddresses[index]),
)
```

> **Regra prática:** Use sempre `ListView.builder` quando a lista for dinâmica ou potencialmente longa. Reserve `ListView` com `children:` apenas para listas estáticas e pequenas, como menus ou formulários com poucos campos fixos.

---

## 7. Como funciona o `addListener()` e o mecanismo de listeners no Flutter?

No projeto, a tela se atualiza automaticamente sempre que o `AddressController` muda seus dados. Isso é possível graças a três peças que trabalham juntas:

```dart
// address_controller.dart
class AddressController extends ChangeNotifier {
  List<AddressModel> addresses = [];

  Future<void> loadAddresses() async {
    // ...
    notifyListeners(); // 1. "Ei, mudei! Quem estiver ouvindo, reaja."
  }
}

// home_page.dart
void initState() {
  super.initState();
  _addressController.addListener(() => setState(() {})); // 2. "Eu quero ser avisado."
  _addressController.loadAddresses();
}
```

### O conceito por trás:

O mecanismo de listeners é uma implementação do padrão **Observer**: um objeto (o *subject*) mantém uma lista de funções cadastradas (os *observers*), e as executa todas quando seu estado muda.

No Flutter, esse padrão é formalizado pela classe `ChangeNotifier`. Ela fornece três operações essenciais:

- **`addListener(fn)`** — cadastra uma função `fn` para ser chamada a cada notificação.
- **`notifyListeners()`** — percorre a lista interna e chama todas as funções cadastradas.
- **`removeListener(fn)`** — remove uma função previamente cadastrada.

O `ChangeNotifier` em si **não sabe nada sobre Flutter ou widgets** — ele é uma classe Dart pura. Quem decide o que fazer ao ser notificado é o listener registrado. No projeto, o listener chama `setState(() {})`, o que aciona o `build()` da tela:

```
_addressController.notifyListeners()
        │
        └─► chama todos os listeners cadastrados
                │
                └─► () => setState(() {})   ← registrado em initState
                            │
                            └─► Flutter agenda um novo build()
                                        │
                                        └─► build() lê _addressController.addresses
                                            e renderiza a lista atualizada
```

### O que é um "listener" na prática:

Um listener é simplesmente uma **função sem parâmetros e sem retorno** (`void Function()`). Quando você escreve:

```dart
_addressController.addListener(() => setState(() {}));
```

Você está criando uma closure anônima e entregando-a ao `ChangeNotifier`. Internamente, ele a armazena em uma lista. Toda vez que `notifyListeners()` é chamado, ele faz o equivalente a:

```dart
for (final listener in _listeners) {
  listener(); // chama cada função cadastrada
}
```

Você pode registrar qualquer função compatível, inclusive um método nomeado:

```dart
void _onControllerChange() {
  setState(() {});
}

// Ambas as formas são equivalentes:
_addressController.addListener(_onControllerChange);
_addressController.addListener(() => setState(() {}));
```

### Por que o `ChangeNotifier` precisa do `dispose()`?

Cada listener registrado é uma referência viva que o `ChangeNotifier` mantém. Se a tela for destruída sem remover o listener, o `AddressController` continuará tentando chamar `setState` em um `State` que já não existe — causando erro em tempo de execução e memory leak.

A solução é remover o listener no `dispose()` do `State`:

```dart
@override
void dispose() {
  _addressController.removeListener(_onControllerChange); // remove o listener
  _addressController.dispose();                           // libera o ChangeNotifier
  _cepController.dispose();
  super.dispose();
}
```

> **Atenção:** Para usar `removeListener`, a função passada precisa ser a **mesma referência** usada no `addListener`. Por isso, lambdas anônimas como `() => setState(() {})` não funcionam bem com `removeListener` — cada vez que você escreve `() => ...`, o Dart cria um objeto closure **novo e diferente**. Para remover corretamente, use um método nomeado.

### Analogia com Java/Android:

É o equivalente ao padrão `Observer` do `LiveData` no Android com ViewModel, mas sem o ciclo de vida gerenciado automaticamente:

| Conceito | Android (LiveData/ViewModel) | Flutter (ChangeNotifier) |
|---|---|---|
| Objeto observável | `LiveData<T>` | `ChangeNotifier` (subclasse) |
| Observar mudanças | `liveData.observe(owner, observer)` | `controller.addListener(fn)` |
| Disparar atualização | `setValue(value)` / `postValue(value)` | `notifyListeners()` |
| Parar de observar | Automático (via `LifecycleOwner`) | `removeListener(fn)` no `dispose()` |
| Reconstruir a UI | `Observer.onChanged()` → manipula views | listener chama `setState(() {})` → `build()` |

A diferença principal é que o `LiveData` gerencia o ciclo de vida automaticamente via `LifecycleOwner`, removendo observers quando a `Activity`/`Fragment` vai para o background. No Flutter, essa responsabilidade é do desenvolvedor — o `dispose()` é o seu `onDestroy`.

### Quando usar `addListener` vs. alternativas:

O `addListener` manual é a abordagem mais explícita e de mais baixo nível. Para casos mais comuns, o Flutter oferece widgets que encapsulam esse mecanismo automaticamente:

| Abordagem | Como funciona | Quando usar |
|---|---|---|
| `addListener` + `setState` | Manual: você registra e gerencia o listener | Aprendizado e controle total |
| `ListenableBuilder` | Widget que se reconstrói automaticamente ao ouvir um `Listenable` | Reconstruir apenas parte da árvore |
| `AnimatedBuilder` | Igual ao `ListenableBuilder`, especializado para animações | Animações com `AnimationController` |
| `ValueListenableBuilder` | Escuta um `ValueNotifier<T>` e expõe o valor diretamente | Estado simples de um único valor |

```dart
// Alternativa com ListenableBuilder — sem precisar de addListener manual:
ListenableBuilder(
  listenable: _addressController,
  builder: (context, child) {
    return ListView.builder(
      itemCount: _addressController.addresses.length,
      itemBuilder: (context, index) => AddressCard(
        address: _addressController.addresses[index],
      ),
    );
  },
)
```

> **Regra prática:** Se você usou `addListener` com uma lambda anônima, lembre-se de que `removeListener` não conseguirá removê-la depois — prefira um método nomeado. E se quiser evitar o gerenciamento manual do listener por completo, use `ListenableBuilder`, que cuida do registro e da remoção automaticamente.

---

## 8. Qual a diferença entre `StatelessWidget` e `StatefulWidget`?

No projeto existem exemplos dos dois tipos lado a lado. `AddressCard` e `AddressListView` são `StatelessWidget`; `HomePage` e `HistoryPage` são `StatefulWidget`. A escolha entre eles é a decisão de design mais frequente no Flutter.

```dart
// StatelessWidget — AddressCard
class AddressCard extends StatelessWidget {
  final AddressModel address;
  final VoidCallback? onDelete;

  const AddressCard({super.key, required this.address, this.onDelete});

  @override
  Widget build(BuildContext context) { ... }
}

// StatefulWidget — HomePage
class HomePage extends StatefulWidget {
  const HomePage({super.key});

  @override
  State<HomePage> createState() => _HomePageState();
}

class _HomePageState extends State<HomePage> {
  final _cepController = TextEditingController(); // estado mutável

  @override
  Widget build(BuildContext context) { ... }

  @override
  void dispose() {
    _cepController.dispose();
    super.dispose();
  }
}
```

### O conceito por trás:

A distinção fundamental é sobre **quem controla quando o widget é redesenhado**.

**`StatelessWidget`** é imutável: todo dado que ele precisa para se renderizar vem pelos parâmetros do construtor (`final`). Ele não tem memória entre rebuilds — cada vez que o Flutter chama `build()`, o widget parte do zero com os dados que recebeu. Se os dados mudarem, o *pai* reconstrói o filho com novos parâmetros.

**`StatefulWidget`** separa o widget (imutável) do seu estado (mutável) em dois objetos distintos. O widget em si (`HomePage`) continua imutável, mas ele cria e mantém um objeto `State` (`_HomePageState`) que **sobrevive entre rebuilds**. Quando `setState()` é chamado, o Flutter descarta a árvore de widgets do `build()` anterior e chama `build()` novamente — mas o objeto `State` e seus campos (`_cepController`) permanecem intactos.

```
StatelessWidget                     StatefulWidget
─────────────────────               ──────────────────────────────────
Widget (imutável)                   Widget (imutável)
  └── build()                         └── createState()
       recebe dados dos pais                  │
       e renderiza                            ▼
                                      State (mutável, sobrevive)
                                        ├── campos: _cepController, etc.
                                        ├── initState()   → chamado 1x ao montar
                                        ├── build()       → chamado a cada setState
                                        └── dispose()     → chamado 1x ao desmontar
```

### O ciclo de vida do `State`:

O objeto `State` tem um ciclo de vida bem definido com três momentos-chave:

- **`initState()`** — executado uma única vez, logo após o `State` ser criado e inserido na árvore. É o lugar certo para inicializar recursos que dependem de `context` ou que precisam existir por toda a vida do widget (ex: controllers, listeners, subscriptions).

- **`build()`** — chamado pelo Flutter toda vez que o widget precisa ser redesenhado: na montagem inicial, após cada `setState()` e quando o pai reconstrói e passa novos parâmetros. Deve ser puro e rápido — sem side effects.

- **`dispose()`** — executado uma única vez, quando o widget é removido permanentemente da árvore (ex: ao navegar para outra tela). É o lugar obrigatório para liberar recursos: `controller.dispose()`, `removeListener()`, cancelar streams.

```dart
// Fluxo completo do ciclo de vida em HomePage:
initState()
  └─► _cepController = TextEditingController()  // recurso criado

build() → build() → build()  // chamado N vezes conforme a UI atualiza

dispose()
  └─► _cepController.dispose()  // recurso liberado
```

### Por que o `StatefulWidget` é dividido em duas classes?

A separação `HomePage` / `_HomePageState` não é burocracia — ela tem um motivo arquitetural. O Flutter pode descartar e recriar o objeto `Widget` (`HomePage`) a qualquer momento durante o processo de reconciliação da árvore, pois widgets são leves e descartáveis. Já o `State` é mantido vivo pelo framework enquanto o widget permanecer na árvore.

Se estado e widget fossem a mesma classe, toda reconstrução apagaria os dados. A separação garante que `_cepController` sobrevive mesmo que o Flutter recrie o objeto `HomePage` internamente.

### `StatelessWidget` com `context.watch` vs. `StatefulWidget`:

Uma dúvida comum é: se `AddressListView` se reconstrói quando o controller muda, por que ela é `StatelessWidget` e não `StatefulWidget`?

A resposta é que **reatividade e estado local são coisas diferentes**. `AddressListView` não possui dados próprios que mudam com o tempo — ela apenas lê o estado que vive em outro lugar (o `AddressController` via Provider). O `context.watch` faz o Provider registrar o rebuild automaticamente, sem precisar de `setState`. Como não há nada para guardar entre rebuilds, `StatelessWidget` é suficiente.

```dart
// StatelessWidget REATIVO — sem estado próprio, mas se reconstrói via Provider
class AddressListView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final controller = context.watch<AddressController>(); // subscrição gerenciada pelo Provider
    // ...
  }
}
```

### Analogia com Java/Android:

| Conceito | Android (XML + Java/Kotlin) | Flutter |
|---|---|---|
| Componente sem estado próprio | `View` estática com dados fixos via `setText` | `StatelessWidget` |
| Componente com estado mutável | `Fragment` com campos e `onViewCreated` | `StatefulWidget` + `State` |
| Inicialização de recursos | `onViewCreated()` / `onCreate()` | `initState()` |
| Atualizar a UI | `invalidate()` / `notifyDataSetChanged()` | `setState(() {})` |
| Liberação de recursos | `onDestroyView()` / `onDestroy()` | `dispose()` |
| Separação widget/estado | `View` (layout) + `ViewModel` (dados) | `StatefulWidget` (config) + `State` (dados) |

### Quando usar cada um:

| Situação | Use |
|---|---|
| Widget só exibe dados recebidos pelo construtor | `StatelessWidget` |
| Widget lê estado externo via Provider/`context.watch` | `StatelessWidget` |
| Widget possui recursos com ciclo de vida (`TextEditingController`, `AnimationController`, streams) | `StatefulWidget` |
| Widget precisa de `initState` ou `dispose` | `StatefulWidget` |
| Widget mantém estado local que muda com interação do usuário (sem gerenciador externo) | `StatefulWidget` |

> **Regra prática:** Comece sempre com `StatelessWidget`. Migre para `StatefulWidget` apenas quando perceber que precisa de `initState`, `dispose` ou de um `setState` para reagir a algo que acontece *dentro* do próprio widget. Se o estado vive fora (Provider, Riverpod, Bloc), o widget que consome pode continuar `StatelessWidget`.

---

## 9. Quando usar `_` (underscore) em variáveis e atribuições no Dart?

No projeto, o underscore aparece em contextos muito diferentes e com significados distintos. Entender cada um evita confusão sobre o que é convenção, o que é privacidade e o que é descarte intencional.

```dart
// address_repository.dart
class AddressRepository {
  static const String _boxName = 'addresses';   // 1. campo privado
  Future<Box<AddressModel>> _openBox() async {  // 1. método privado
    return await Hive.openBox<AddressModel>(_boxName);
  }
}

// address_controller.dart
class AddressController extends ChangeNotifier {
  final AddressRepository _repository = AddressRepository(); // 1. campo privado
  final List<AddressModel> _staticAddresses = [...];         // 1. campo privado
}

// home_page.dart
class HomePage extends StatefulWidget {
  State<HomePage> createState() => _HomePageState(); // 2. classe privada
}

class _HomePageState extends State<HomePage> {       // 2. classe privada
  final _cepController = TextEditingController();    // 1. campo privado

  TextField(
    onSubmitted: (_) { _searchCep(); }, // 3. parâmetro descartado
  )
}
```

### O conceito por trás:

O underscore tem **três usos independentes** no Dart, cada um com regra e motivação própria.

---

### Uso 1 — Privacidade de biblioteca (`_nome`)

Em Dart, **não existem modificadores `private`, `protected` ou `public`**. A privacidade é controlada exclusivamente pelo prefixo `_`: qualquer identificador que começa com `_` é visível apenas dentro da **biblioteca** (arquivo `.dart`) em que foi declarado.

```dart
class AddressRepository {
  static const String _boxName = 'addresses'; // só visível dentro de address_repository.dart
  Future<Box<AddressModel>> _openBox() async { ... } // idem
}
```

Tentar acessar `_boxName` ou `_openBox()` de outro arquivo resulta em erro de compilação. Isso vale para campos, métodos, getters, setters e até classes inteiras — como `_HomePageState`, que é privada ao arquivo `home_page.dart` e não pode ser instanciada de fora.

A unidade de privacidade é o **arquivo**, não a classe. Isso significa que duas classes no mesmo arquivo enxergam os membros `_` uma da outra — comportamento diferente do Java, onde `private` é por classe.

```dart
// Mesmo arquivo → acesso permitido
class Foo {
  int _valor = 42;
}

class Bar {
  void lerFoo(Foo f) => print(f._valor); // compila, pois Bar e Foo estão no mesmo arquivo
}
```

```dart
// Arquivos diferentes → acesso bloqueado
import 'foo.dart';
class Bar {
  void lerFoo(Foo f) => print(f._valor); // ERRO: '_valor' is not accessible
}
```

---

### Uso 2 — Convenção para membros de `State` (`_campo` no `StatefulWidget`)

Nos `State` do Flutter, é uma convenção forte prefixar os campos com `_` mesmo quando a privacidade técnica não seria estritamente necessária. Isso comunica intenção: este campo **não deve ser acessado nem modificado de fora deste `State`**.

```dart
class _HomePageState extends State<HomePage> {
  final _cepController = TextEditingController(); // privado ao State
}
```

A classe `_HomePageState` já é privada ao arquivo. Mesmo assim, `_cepController` recebe `_` para reforçar que é um detalhe de implementação interno — nenhuma outra parte do código deveria precisar ler ou setar este controller diretamente.

---

### Uso 3 — Parâmetro descartado (`_`)

Quando uma função de callback exige um parâmetro que você não vai usar, o Dart permite nomeá-lo `_` para sinalizar explicitamente que o valor é ignorado:

```dart
TextField(
  onSubmitted: (_) {   // o parâmetro é o texto digitado (String), mas já lemos via _cepController.text
    _searchCep();
  },
)
```

Usar `_` como nome de parâmetro é uma **convenção semântica**: diz ao leitor "sei que esse parâmetro existe, mas escolhi não usá-lo". Isso é mais honesto do que nomear `value` ou `text` e nunca referenciar.

Se houver múltiplos parâmetros descartados, o Dart permite repetir o padrão com `__` (dois underscores) ou `_1`, `_2` — mas na prática isso é raro:

```dart
someWidget.addListener((_, __) => doSomething()); // dois parâmetros ignorados
```

> **Atenção:** `_` como nome de variável **é diferente** de `_nome` como prefixo. `_` sozinho é um identificador válido (descarte), enquanto `_nome` é privacidade. O compilador trata os dois de formas distintas.

---

### Comparação dos três usos:

| Forma | O que faz | Onde aparece no projeto |
|---|---|---|
| `_campo` / `_metodo()` | Torna o membro privado à biblioteca (arquivo) | `_boxName`, `_openBox()`, `_repository`, `_staticAddresses`, `_cepController` |
| `_NomeDeClasse` | Torna a classe privada à biblioteca | `_HomePageState` |
| `(_)` em callback | Sinaliza que o parâmetro é intencionalmente ignorado | `onSubmitted: (_) { _searchCep(); }` |

### Analogia com Java/Android:

| Conceito Dart | Equivalente Java | Diferença |
|---|---|---|
| `_campo` (privado) | `private int campo` | Em Java, `private` é por *classe*; em Dart, `_` é por *arquivo* |
| Sem `_` (público) | `public int campo` | Dart não tem `public` — ausência de `_` já significa público |
| Sem `protected` | `protected` não existe | Dart não tem herança de privacidade; subclasses em outros arquivos não enxergam `_` |
| `(_)` descartado | `Runnable r = (ignored) -> doSomething()` | Mesma intenção, Dart usa `_` por convenção |

> **Regra prática:** Use `_` em tudo que for detalhe de implementação interna: campos de `State`, métodos auxiliares, constantes de configuração. Omita o `_` apenas em membros que fazem parte da API pública da classe — aqueles que você *quer* que outros arquivos acessem. Para parâmetros não utilizados em callbacks, prefira sempre `_` a nomes genéricos como `value` ou `ignored`.

---

## 10. O que são as intents declaradas no `AndroidManifest.xml`?

No projeto, o `AndroidManifest.xml` contém declarações que vão além do mínimo para um app Flutter funcionar. Entender cada bloco é importante porque qualquer funcionalidade que precise abrir um app externo (mapas, navegação) depende dessas declarações para funcionar no Android 11+.

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application ...>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:configChanges="orientation|keyboardHidden|..."
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="google.navigation"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="waze"/>
        </intent>
    </queries>
</manifest>
```

### O conceito por trás:

Uma **Intent** no Android é uma mensagem assíncrona que descreve uma operação a ser realizada — ela é o mecanismo central de comunicação entre componentes (activities, services, broadcast receivers) dentro do mesmo app ou entre apps diferentes. Em vez de chamar código diretamente, você descreve *o que quer fazer*, e o Android encontra quem sabe fazer.

O `AndroidManifest.xml` é o contrato entre o app e o sistema operacional. Tudo que o app é capaz de fazer ou precisar fazer com outros apps deve estar declarado aqui antes de acontecer em tempo de execução.

---

### Bloco 1 — `<intent-filter>` dentro da `<activity>`: ponto de entrada do app

```xml
<intent-filter>
    <action android:name="android.intent.action.MAIN"/>
    <category android:name="android.intent.category.LAUNCHER"/>
</intent-filter>
```

Este `intent-filter` diz ao Android duas coisas:

- **`ACTION_MAIN`** — esta `Activity` é o **ponto de entrada principal** do app. Sem essa action, o Android não sabe por onde começar ao abrir o app.
- **`CATEGORY_LAUNCHER`** — o app deve aparecer na **gaveta de apps** do dispositivo com ícone e nome. Sem essa category, o app seria instalado mas invisível para o usuário no launcher.

A combinação dos dois é o mínimo obrigatório para qualquer app Android aparecer e ser iniciado pelo usuário. O Flutter gera isso automaticamente, mas é importante saber que removê-los tornaria o app "sem ícone" — instalável via `adb`, mas inacessível ao usuário final.

---

### Atributos relevantes da `<activity>`:

Além do `intent-filter`, a `MainActivity` tem atributos que o Flutter configura por razões específicas:

**`android:exported="true"`** — a activity pode ser iniciada por componentes de *outros* apps (não só do próprio app). É obrigatório quando a activity tem um `intent-filter`, pois filtros são, por definição, intenções públicas. A partir do Android 12, o sistema exige que esse atributo seja declarado explicitamente — omiti-lo causa erro de build.

**`android:launchMode="singleTop"`** — se a `MainActivity` já estiver no topo da pilha de activities, o Android não cria uma nova instância; em vez disso, entrega a intent para a instância existente via `onNewIntent()`. O Flutter usa isso para receber deep links sem duplicar a tela.

**`android:taskAffinity=""`** — remove a afinidade padrão de tarefa (que seria o nome do pacote). Com valor vazio, a activity não tem preferência por nenhuma tarefa específica, o que evita comportamentos inesperados de agrupamento quando o app é iniciado por deep links ou notificações externas.

**`android:configChanges="orientation|keyboardHidden|keyboard|screenSize|..."`** — lista de mudanças de configuração que a activity trata **por conta própria**, sem pedir ao Android para recriá-la. Normalmente, girar o dispositivo ou mostrar o teclado destruiria e recriaria a `Activity` — o Flutter intercepta essas mudanças internamente e redesenha a UI pelo seu próprio mecanismo, então a recriação do Android seria redundante e causaria flickering.

**`android:windowSoftInputMode="adjustResize"`** — quando o teclado virtual aparece, a janela do app é **redimensionada** para caber acima do teclado. A alternativa seria `adjustPan` (a janela se desloca sem redimensionar). O Flutter precisa do `adjustResize` para que o `MediaQuery.of(context).viewInsets` reflita corretamente o espaço ocupado pelo teclado, o que permite que campos de texto subam automaticamente ao ficarem ocultos pelo teclado.

---

### Bloco 2 — `<queries>`: visibilidade de pacotes (Android 11+)

```xml
<queries>
    <intent>
        <action android:name="android.intent.action.PROCESS_TEXT"/>
        <data android:mimeType="text/plain"/>
    </intent>
    <intent>
        <action android:name="android.intent.action.VIEW"/>
        <data android:scheme="geo"/>
    </intent>
    ...
</queries>
```

A partir do Android 11 (API 30), o sistema aplica **Package Visibility**: por padrão, um app não consegue enxergar nem interagir com outros apps instalados no dispositivo — a menos que declare explicitamente quais intents pretende consultar ou disparar. Isso é uma medida de privacidade para evitar que apps mapeiem todos os outros apps instalados.

O bloco `<queries>` declara as intents que este app **precisa consultar** (via `PackageManager.queryIntentActivities()` ou `canResolveActivity()`). Sem essa declaração, o Android retorna lista vazia ou `false` mesmo que o app-alvo esteja instalado.

As quatro intents declaradas e seus propósitos:

| Intent | Scheme / MimeType | Para que serve |
|---|---|---|
| `ACTION_PROCESS_TEXT` | `text/plain` | Permite que o Flutter Engine ofereça o texto selecionado na tela para apps externos (tradutores, buscadores). Usado internamente pelo `ProcessTextPlugin` do Flutter. |
| `ACTION_VIEW` | `geo:` | Abre um local geográfico em qualquer app de mapas instalado (Google Maps, Here Maps, etc.). O scheme `geo:lat,lng` é o padrão Android para coordenadas. |
| `ACTION_VIEW` | `google.navigation:` | Inicia navegação turn-by-turn diretamente no **Google Maps**. O scheme proprietário `google.navigation:q=endereço` pula a tela de busca e abre a rota. |
| `ACTION_VIEW` | `waze:` | Inicia o **Waze** com um destino. O scheme `waze://?q=endereço` é proprietário do Waze. |

As três últimas intents (`geo`, `google.navigation`, `waze`) são diretamente relevantes ao propósito do projeto — um app de busca de endereços que precisará, futuramente, oferecer ao usuário a opção de abrir o endereço encontrado em um app de navegação.

#### Como o Android resolve uma intent em tempo de execução:

```
app dispara Intent(ACTION_VIEW, Uri.parse("geo:0,0?q=Praça da Sé"))
        │
        ▼
Android verifica apps instalados que têm intent-filter para ACTION_VIEW + scheme "geo"
        │
        ├─► Nenhum app encontrado  →  ActivityNotFoundException (app crasha se não tratar)
        │
        └─► Um ou mais apps encontrados
                ├─► Apenas um  →  abre diretamente
                └─► Vários     →  exibe o chooser ("Abrir com...")
```

Sem a declaração no `<queries>`, a verificação prévia via `canLaunchUrl()` (do pacote `url_launcher`) retornaria `false` mesmo com o Google Maps instalado — e a tentativa de abrir causaria exception.

### Analogia com Java/Android nativo:

Para quem vem do Android nativo, o `AndroidManifest.xml` do Flutter é idêntico em estrutura — a diferença é que existe apenas uma `Activity` (`MainActivity`) que hospeda o motor Flutter, em vez de múltiplas Activities para cada tela. Toda a navegação entre telas (`HomePage` → `HistoryPage`) acontece dentro do Flutter, não entre Activities Android.

| Conceito Android | Equivalente Flutter |
|---|---|
| Múltiplas `<activity>` no Manifest | Uma única `MainActivity` + rotas Flutter (`/`, `/history`) |
| `startActivity(intent)` | `Navigator.pushNamed(context, '/history')` |
| `intent-filter` para deep link | Idem — declarado no Manifest, tratado pelo Flutter via `GoRouter` ou `Navigator` |
| `<queries>` para abrir outros apps | Idem — necessário antes de usar `url_launcher` ou `Intent` explícita |

---

## 11. O que é `context.mounted`?

`context.mounted` é uma propriedade do `BuildContext` que indica se o widget ao qual aquele contexto pertence ainda está presente na árvore de widgets — ou seja, se ele ainda está "montado" na tela.

### Por que isso importa em operações assíncronas?

O problema clássico surge quando você executa uma operação `async` e, depois do `await`, tenta usar o `context` para navegar, exibir um `SnackBar` ou chamar `setState`. Entre o início e o fim da operação assíncrona, o widget pode ter sido removido da tela (o usuário navegou para outra página, fechou o app, etc.).

Se você usar o `context` após o widget ter sido desmontado, o Flutter emitirá um aviso em debug:

```
This widget has been unmounted, so the State no longer has a context
```

E em casos mais graves, pode causar erros em runtime.

### Exemplo do problema:

```dart
Future<void> _buscarEndereco() async {
  final resultado = await _enderecoService.buscar(_cepController.text);

  // ⚠️ Perigoso: o widget pode ter sido destruído durante o await acima
  Navigator.of(context).push(...);
}
```

### A solução — verificar `context.mounted`:

```dart
Future<void> _buscarEndereco() async {
  final resultado = await _enderecoService.buscar(_cepController.text);

  // ✅ Seguro: só usa o context se o widget ainda estiver na tela
  if (!context.mounted) return;
  Navigator.of(context).push(...);
}
```

### Como `mounted` funciona internamente:

O Flutter mantém internamente um flag booleano no `State` do widget. Ele é definido como `true` quando o widget é inserido na árvore (`initState`) e como `false` quando é removido (`dispose`). A propriedade `context.mounted` expõe esse flag diretamente no `BuildContext`, disponível desde o Flutter 3.7.

| Momento | Valor de `mounted` |
|---|---|
| Widget visível na tela | `true` |
| Após `Navigator.pop()` / troca de rota | `false` |
| Após `dispose()` ser chamado | `false` |

### Regra prática:

> Sempre que usar `context` após um `await`, adicione um `if (!context.mounted) return;` antes.

> **Regra prática:** Sempre que precisar abrir um app externo (mapas, browser, telefone, e-mail), declare a intent correspondente no bloco `<queries>` antes de codificar. Sem a declaração, `canLaunchUrl()` retorna `false` no Android 11+ mesmo com o app instalado, e a tentativa de abrir sem verificar lança exception em produção.
